---
const { href = '#', title, description } = Astro.props;
---
<a href={href} class="block rounded-2xl p-5 bg-[color:var(--card)] border border-white/10 hover:translate-y-[-2px] transition">
  <h3 class="text-lg font-semibold mb-1">{title}</h3>
  <p class="text-sm text-[color:var(--muted)]">{description}</p>
</a>
